package com.gemini.{{scriptName}}.model;
import java.time.LocalDate;

public class {{className}} {

    {{#each primaryKeys}}private {{datatype}} {{fieldName}};
    {{/each}}

    {{#each variable}}private {{datatype}} {{fieldName}};
    {{/each}}

    {{#each primaryKeys}}public {{datatype}} get{{fieldName}}() {
            return {{fieldName}};
        }
    {{/each}}
    {{#each variable}}public {{datatype}} get{{fieldName}}() {
        return {{fieldName}};
    }
    {{/each}}

    {{#each primaryKeys}}public void set{{fieldName}}({{datatype}} {{fieldName}}) {
            this.{{fieldName}} = {{fieldName}};
        }
    {{/each}}
    {{#each variable}}public void set{{fieldName}}({{datatype}} {{fieldName}}) {
        this.{{fieldName}} = {{fieldName}};
    }
    {{/each}}

    @Override
    public String toString() {
        return "{{className}}{" +
                {{#each primaryKeys}}"{{fieldName}}=" + {{fieldName}} +{{/each}}
                {{#each variable}}"{{fieldName}}=" + {{fieldName}} +{{/each}}
                '}';
    }
}